## Phase S2-E1 — SQLite Migration Compiler & Controlled Dry Run

تضيف S2-E1 Compiler مضبوطًا لنسخة SQLite الحقيقية: يتحقق من سلامة القاعدة والعلاقات، يغطي جداول المصدر الـ25 والهدف الـ26، يطبق تحويلات السنوات و`teacher_years` ودمج علاقات Legacy، يستبعد الأسرار بتدقيق صريح، ويولد SQL تجريبيًا ينتهي إلزاميًا بـ`ROLLBACK;`. لا توجد Migration جديدة ولا تغيير Schema/RLS/Runtime أو نقل ملفات فعلية. راجع `SUPABASE_SQLITE_MIGRATION_DRY_RUN_AR.md`.

## Phase S2-D Fix 1 — Acceptance Fixture Cleanup

صحح هذا الإصدار اختبار S2-D فقط. اختبار `ON DELETE SET NULL` كان ينشئ مستندًا مؤقتًا ثالثًا مرتبطًا بالمعلم ثم يتركه داخل نفس Transaction، بينما اختبارات دور `teacher` و`lead_teacher` كانت تقيس المستندين الأساسيين فقط. بعد إثبات أن `documents.request_id` أصبح `NULL` يُحذف المستند المؤقت قبل اختبارات الأدوار. لا توجد Migration أو تغييرات Schema/RLS/Runtime.

## Phase S2-D — Database Acceptance & Migration Readiness

أغلقت هذه المرحلة بوابة قبول قاعدة Supabase دون إضافة Migration جديدة: اختبار حي مترابط عبر الجداول الـ26، عزل مدرستين، عامين دراسيين، أدوار Auth/RLS، القيود المرجعية والحسابية، و`updated_at`. كما ثُبت Manifest لنقل جداول SQLite الـ25 إلى الهدف دون إسقاط صامت أو اختلاق بيانات تاريخية. نجاح S2-D يعني الجاهزية لـData Migration Dry Run فقط، وليس قطع FastAPI/SQLite. راجع `SUPABASE_DATABASE_ACCEPTANCE_AR.md`.

## Phase S2-C2 — Domain RLS Baseline

اكتملت طبقة RLS الأساسية على الجداول الـ21 المتبقية. القراءة تفرق بين السجلات التشغيلية المدرسية والسجلات الخاصة بالمعلم/الوثائق، والكتابة المباشرة محصورة في `owner/admin` وبالعمليات المثبتة في Legacy. تبقى `teacher_years`, `upload_requests`, `documents`, `event_media`, و`activities` بلا كتابة مباشرة من المتصفح حتى المسارات الموثوقة/Storage اللاحقة. لا يزال Runtime على FastAPI/SQLite. راجع `SUPABASE_DOMAIN_RLS_BASELINE_AR.md`.

## Phase S2-C1 — Auth + RLS Security Foundation

بدأت طبقة التفويض الفعلية على خمسة جداول نواة فقط: `schools`, `profiles`, `school_memberships`, `academic_years`, و`school_settings`. مصدر الدور الموثوق هو `school_memberships.role` فقط، ودوال RLS المساعدة داخل schema `private` غير المعروضة للـData API. لا توجد كتابة مباشرة للعضويات من المتصفح، وتبقى الجداول الـ21 الأخرى مقفلة حتى S2-C2. التشغيل ما زال FastAPI/SQLite. راجع `SUPABASE_SECURITY_FOUNDATION_AR.md`.

## Phase S2-B5 Fix 1 — updated_at clock

أثبت الاختبار الحي أن `statement_timestamp()` ثابت داخل SQL statement واحد، لذلك أضيفت Migration تصحيحية مستقلة تستخدم `clock_timestamp()` مع إعادة استخدام الـ22 Trigger القائمة.

## Phase S2-B5 — Final Schema Constraints & Acceptance

أغلقت هذه المرحلة مخطط PostgreSQL المستهدف: 26/26 جدولًا و299 عمودًا مطابقًا لعقد S2-A، مع تدقيق عام لعزل `school_id`، وإضافة 22 trigger موحدًا لـ`updated_at` وثلاثة فهارس نهائية فقط. لا يوجد نقل بيانات أو Auth/RLS Policies بعد. راجع `SUPABASE_SCHEMA_CLOSURE_AR.md`.

## Phase S2-B4 — Events / Documents / Requests / Activities

أضيفت migration رابعة تكمل الجداول السبعة المتبقية من عقد S2-A: `school_settings`, `upload_requests`, `documents`, `events`, `event_media`, `event_teacher_links`, و`activities`. جداول Legacy الخاصة بربط السنة وmetadata لا يعاد إنشاؤها؛ تُدمج وظيفتها داخل التصميم الجديد. لا يزال التشغيل FastAPI/SQLite ولا يوجد نقل بيانات أو ملفات تخزين في هذه المرحلة. راجع `SUPABASE_CONTENT_INTAKE_MIGRATION_AR.md`.

## Phase S2-B3 — Operational Domains Migration

أضيفت migration ثالثة تنقل بنية الاجتماعات والتخطيط والإشراف والتحصيل إلى PostgreSQL عبر 11 جدولًا جديدًا، مع عزل المدرسة والسنة الدراسية وعلاقات المعلمين المركبة. التشغيل ما زال FastAPI/SQLite وRLS مؤجل إلى S2-C. راجع `SUPABASE_OPERATIONAL_MIGRATION_AR.md`.

## Phase S2-B2 — Teachers Domain Migration

أضيفت migration ثانية لمجال المعلمين: `teachers`, `teacher_profiles`, `teacher_years`, و`teacher_cv_items`، مع إكمال ربط `school_memberships.teacher_id` بالمعلم داخل المدرسة نفسها. التشغيل ما زال FastAPI/SQLite وRLS مؤجل إلى S2-C.

# مرصد الإنجازات v0.25.0

**مرصد الإنجازات** منصة عربية RTL لإدارة أعمال المادة وبناء ذاكرة مؤسسية للمعلم الأول ورئيس المجال. الفكرة ليست جمع ملفات في شاشة واحدة، بل ربط الأشخاص والأعمال والقرارات والتخطيط والتحصيل والإشراف والفعاليات والوثائق في سجلات قابلة للمتابعة والتقرير.

## Phase S2-B1 — Core Identity & Tenancy

أضيفت أول migration PostgreSQL فعلية للجداول الأساسية `schools`, `profiles`, `school_memberships`, و`academic_years` وفق عقد S2-A المجمد. لا تزال الواجهة تعمل على FastAPI/SQLite، ولا يوجد نقل بيانات أو RLS في هذه المرحلة. الجداول الجديدة مقفلة عن `anon` و`authenticated` افتراضيًا حتى S2-C. راجع `SUPABASE_CORE_MIGRATION_AR.md`.

## Phase S2-A — تجميد تصميم PostgreSQL

أُغلق تصميم قاعدة Supabase المستهدفة قبل كتابة SQL: **25 جدول Legacy → 26 جدول PostgreSQL مستهدف**، مع `schools` و`profiles` و`school_memberships` و`academic_years`، وفصل هوية المعلم المستمرة عن حالته السنوية في `teacher_years`. لا توجد migration تشغيلية في S2-A، ولا تزال الواجهة تعمل على FastAPI/SQLite. مصدر الحقيقة: `SUPABASE_SCHEMA_FREEZE_AR.md` و`supabase/schema/target_schema_v1.json`.

> **Railway لم يُستخدم إنتاجيًا في المشروع.** الملفات الخاصة به تبقى Legacy مؤقتًا ولا تدخل خطة البيانات أو Supabase.

## Phase S1 — تأسيس Supabase دون تحويل التشغيل

بدأ مسار الانتقال من SQLite/FastAPI إلى Supabase بإضافة عميل الويب، إعداد CLI المحلي، متغيرات البيئة الآمنة، ومجلدات migrations/functions/tests. **لا يوجد SQL جديد ولا جدول منقول في S1، ولا تزال الواجهة تعمل على FastAPI/SQLite كما في S0.** يمنع فحص S1 إدخال Secret/Service Role إلى الواجهة أو استهلاك Supabase من صفحات الإنتاج قبل بوابة التكافؤ. راجع `SUPABASE_FOUNDATION_AR.md`.


## ما يعمل حاليًا

- لوحة قيادة تعتمد على البيانات الفعلية للأعمال التي تحتاج انتباهًا.
- ملفات مهنية للمعلمين وسيرة ذاتية منظمة.
- طلبات ملفات وروابط رفع خارجية مع مراجعة واعتماد.
- توثيق فعاليات بالأدلة والصور والمشاركين.
- اجتماعات وقرارات ومسؤوليات ومواعيد متابعة.
- تخطيط ومتابعة منهج ووحدات ونسب إنجاز وتأخر.
- إشراف فني وزيارات وإجراءات متابعة.
- تحصيل ونتائج وتدخلات علاجية وإثرائية.
- مكتبة وثائق مرتبطة بالسياق التربوي.
- Google Drive اختياري كمخزن فعلي للملفات عبر OAuth.
- GitHub Pages كمعاينة آمنة ببيانات نموذجية دون تزييف عمليات الحفظ.
- أرشيف تاريخي سنوي للقراءة فقط مع حزمة تسليم قابلة للطباعة.
- بحث شامل عربي من رأس التطبيق مع فلاتر للقسم والعام الدراسي وتنقل مباشر إلى السجل الأصلي.

## جديد v0.14-B — Railway والتشغيل الحقيقي

اختير Railway لبيئة التشغيل الحقيقية الأولى. المشروع يملك الآن `Dockerfile` و`railway.json`، ويعمل كنطاق واحد يخدم React وFastAPI معًا. عند ربط Volume يكتشف المرصد `RAILWAY_VOLUME_MOUNT_PATH` ويضع تحته قاعدة SQLite والنسخ والملفات المحلية تلقائيًا.

كما أضيفت **بوابة دخول إنتاجية مؤقتة** عبر `APP_ACCESS_PASSWORD` حتى لا يصبح رابط Railway العام بابًا مفتوحًا لسجلات المدرسة. روابط رفع المعلمين المحمية بالـtoken لا تتطلب جلسة الإدارة. هذه البوابة أحادية المستخدم مؤقتة إلى أن تُبنى الحسابات والصلاحيات الكاملة.

راجع [`RAILWAY_DEPLOYMENT_AR.md`](RAILWAY_DEPLOYMENT_AR.md) و`RAILWAY_ENV_TEMPLATE.txt`. لا يوجد SQL جديد ولا تعديل لأي قاعدة تربوية.

## جديد v0.14-A — جاهزية التشغيل الإنتاجي والنسخ الاحتياطي

هذه المرحلة تنقل المشروع من «كود يملك Backend» إلى **مشروع جاهز للنشر الفعلي** دون تغيير المنطق التربوي أو اختيار منصة استضافة بعينها.

- `VITE_API_BASE_URL` يسمح للواجهة بالعمل مع FastAPI على نفس النطاق أو على نطاق API منفصل.
- CORS يُقرأ من `APP_CORS_ORIGINS` ولا يعتمد على Wildcard تلقائي.
- `/api/health` للفحص الحي، و`/api/ready` لجاهزية قاعدة البيانات ومجلدات البيانات والنسخ والرفع. في `APP_ENV=production` يجب تعريف `APP_DATA_DIR` و`APP_BACKUP_DIR` صراحةً، وكذلك مجلدا الرفع عند استخدام `local/auto`.
- نسخة SQLite آمنة تُنشأ عند بدء التشغيل قبل `init_db()` عندما توجد قاعدة سابقة، باستخدام SQLite Backup API مع سياسة احتفاظ.
- `scripts/marsad_maintenance.py` يدعم `backup` و`verify` و`restore`. الاستعادة Offline فقط وتُنشئ نسخة `pre-restore` قبل الاستبدال.
- روابط رفع المعلمين تستخدم عنوان الواجهة، وعناوين أدلة الفعاليات تستخدم عنوان الـAPI الصحيح عند فصل الواجهة عن الخادم.
- GitHub Pages تبقى Preview ولا تتحول إلى مخزن بيانات.

راجع [`PRODUCTION_DEPLOYMENT_AR.md`](PRODUCTION_DEPLOYMENT_AR.md) و`PRODUCTION_ENV_TEMPLATE.txt`. لا توجد ترقية SQLite جديدة في v0.14-A ولا تعديل لأي قاعدة تربوية عُمانية.

## جديد v0.13.1 — تقويم الأعوام ودليل المعلمين التاريخي

صححت هذه المرحلة تجربة الإدخال التاريخي بعد الاختبار الفعلي. لم يعد «عام العمل» حقلًا نصيًا؛ أصبح محدد أعوام دراسية يعرض السنوات المتتالية ويميز العام الجاري والعام المختار والأعوام التي تحتوي بيانات، مع تنقل إلى أعوام أقدم أو أحدث وعودة مباشرة للعام الجاري.

كما فُصلت قائمة **معلمي سنة العمل** عن **دليل المعلمين الرئيسي**:

- `teachers` يبقى خاصًا بالمعلمين المرتبطين بالسنة المختارة، حتى تظل العدادات والبيانات السنوية صحيحة.
- `teacherDirectory` يعرض هويات المعلمين الموجودة في المرصد عبر السنوات، ويغذي قوائم الاختيار في السجلات التاريخية.
- إذا اختير معلم من الدليل لاجتماع أو خطة أو زيارة أو تقويم أو فعالية أو وثيقة في سنة سابقة، يربطه الخادم بتلك السنة تلقائيًا دون إنشاء معلم مكرر.
- نموذج الاجتماع الجديد يستخدم اختيار أسماء متعددًا للحضور مع البحث، وتحديد الكل، وإلغاء الكل، وعدد المختارين.
- حقول العام الدراسي في نماذج السجلات تتبع عام مساحة العمل وتظهر للقراءة فقط، بدل السماح بكتابة سنة مختلفة عن السياق.

في GitHub Pages تظل عمليات الحفظ معطلة لأنها معاينة فقط، لكن دليل المعلمين يبقى متاحًا في السنة التاريخية حتى يمكن فحص تجربة اختيار الحضور والمسؤولين.

لا يحتاج v0.13.1 إلى SQL يدوي أو Secret جديد أو تعديل OAuth أو GitHub Workflow، ولا يضيف جدول قاعدة بيانات جديدًا.

## جديد v0.13.0 — الإدخال التاريخي والاستمرارية الطولية

يمكن الآن إدخال أعمال السنوات السابقة ثم مواصلة البناء عليها في السنوات الجديدة دون تحويل الأرشيف إلى قاعدة بيانات موازية. يفرّق التطبيق بين **عام التشغيل الجاري** و**عام السجل**، ويتيح اختيار عام العمل من أعلى الواجهة أو كتابة عام سابق جديد بصيغة `2024/2025`.

- التخطيط والتحصيل والإشراف والاجتماعات والفعاليات تحمل عام السجل صراحةً.
- لوحة القيادة والتنبيهات والمؤشرات تبقى محصورة في عام العمل المختار.
- فتح نتيجة بحث من سنة أخرى ينقل مساحة العمل أولًا إلى عام النتيجة.
- إدخال سجل قديم اليوم لا يجعله نشاطًا للعام الجاري؛ النشاط يتبع سنة الكيان المرتبط به.
- ارتباط المعلم بالسنة أصبح صريحًا أيضًا؛ يمكن إضافة معلم لعام سابق دون إدخاله تلقائيًا ضمن فريق العام الجاري، مع بقاء ملفه المهني الأساسي هوية مستمرة لا لقطة تاريخية مختلقة.
- زر **إضافة وثيقة** أصبح يرفع ملفًا مباشرًا إلى سنة محددة دون إنشاء طلب ملف تاريخي وهمي.
- Google Drive، عند تفعيله، يحفظ الوثائق المباشرة في مجلد سنة السجل تحت `02 - الوثائق والمراجع`.

للحفاظ على عقود النسخ القديمة لم تتغير جداول الأحداث أو الطلبات، وأضيف فقط:

```text
request_record_years
event_record_years
teacher_record_years
```

السجلات الجديدة تحفظ عامها صراحةً. السجلات الأقدم التي لا تحمل هذا الربط تبقى دون تعديل، ويستخدم تاريخها كآلية توافق خلفي فقط.

### حارس المقارنة الطولية

جمع عدة سنوات لا يساوي تلقائيًا صلاحية المقارنة التربوية بينها. مقارنة الإتقان أو انتشار الإتقان أو أثر البرامج عبر السنين تحتاج توافق المرجع والأداة وطريقة الحساب والمعيار العُماني بين الفترات. التطبيق لا يفترض هذا التكافؤ ولا ينشئ حكمًا تربويًا من مجرد وجود رقمين في سنتين مختلفتين. وحتى داخل السنة الواحدة، لا تُجمع نسب الفئة المحققة للحد إذا اختلف الحد أو مصدر المرجع أو إصداره بين التقويمات؛ تعرض «غير مجمعة» بدل حساب غير صالح تربويًا.

التحقق من التاريخ بنيوي فقط: يجب أن تكون السنة الميلادية للتاريخ واحدة من سنتي العام الدراسي المسجل، دون افتراض حدود بداية أو نهاية الفصول الدراسية العُمانية من عندنا.

لا يحتاج v0.13.0 إلى SQL يدوي أو Secret جديد أو تعديل OAuth.

## جديد v0.12.0 — دورة التدخل وقياس الأثر

تطورت التدخلات العلاجية والإثرائية داخل **التحصيل والنتائج** إلى دورة متابعة قابلة لقياس الأثر دون إنشاء وحدة مكررة. يمكن ربط كل إجراء بمقياس منظم: اسم المؤشر، الوحدة، اتجاه التحسن، خط الأساس، الهدف المسجل، النتيجة الفعلية، تاريخ القياس، ومصدر الهدف أو المعيار.

### قاعدة عدم اختراع المعايير

- لا يقترح التطبيق حدًا افتراضيًا جديدًا للتصنيف/الإتقان عند تسجيل نتيجة.
- يلزم توثيق مصدر الحد المستخدم في التقويم الجديد أو المعدل.
- يلزم توثيق مصدر الهدف أو المعيار عند تفعيل قياس أثر رقمي.
- نتيجة قياس الأثر حسابية فقط بالنسبة للهدف المسجل ولا تعني تلقائيًا حكمًا تربويًا وزاريًا.
- السجلات القديمة التي سبقت حقول المصدر لا تُعدّل ولا يُختلق لها مرجع؛ تظهر بوضوح بوصفها بلا مرجع موثق حتى تتم مراجعتها.

الحالات الحسابية الممكنة: **لم يُقَس بعد، حقق الهدف المسجل، تحسن ولم يبلغ الهدف، لم يحدث تغير، تراجع المؤشر**.

الجداول الجديدة تضاف تلقائيًا وبصورة محافظة على قاعدة v0.11:

```text
achievement_assessment_standards
achievement_action_metrics
```

لا يلزم SQL يدوي أو Secret جديد أو تعديل Google Drive.

## جديد v0.11.0 — البحث الشامل والتنقل السريع

تحول مربع **«ابحث في أعمال المادة...»** إلى محرك بحث فعلي يعمل على البيانات الأصلية نفسها دون جدول فهرسة موازٍ.

### نطاق البحث

- المعلمون والبيانات المهنية والسيرة الذاتية.
- الخطط ووحدات المنهج.
- التحصيل والتدخلات.
- الإشراف وإجراءات المتابعة.
- طلبات الملفات.
- الاجتماعات والقرارات.
- الفعاليات والمشاركون.
- الوثائق والمراجع.

### خصائص البحث

- تطبيع عربي للهمزات والتشكيل والتطويل.
- لا يرسل استعلامًا قبل إدخال حرفين على الأقل.
- تصفية حسب القسم والعام الدراسي.
- اختصار `Ctrl/Cmd + K`.
- ترتيب النتائج حسب الصلة ثم الحداثة.
- النتائج الفرعية مثل القرار أو وحدة المنهج تفتح السجل الأب المرتبط بها.
- يعمل في GitHub Pages بعقد معاينة مطابق.

```text
GET /api/search?q=...&section=all&academicYear=all&limit=40
```

المسار للقراءة فقط، ولا يبحث في نصوص التقارير المولدة لأنها بيانات مشتقة. لا يضيف v0.11.0 جدول SQLite أو Secret جديدًا ولا يغير إعداد Google Drive.

## جديد v0.10.0 — الأرشيف التاريخي وحزمة التسليم

أصبح تبويب **الأرشيف التاريخي** وحدة فعلية بدل placeholder. يكتشف الأعوام الدراسية تلقائيًا من السجلات الحالية ويجمع بيانات كل عام دون إنشاء نسخة موازية من البيانات.

### ما يعرضه الأرشيف

- التخطيط والمنهج.
- التحصيل والنتائج.
- الإشراف والمتابعة.
- الاجتماعات والقرارات.
- الفعاليات والتوثيق.
- الوثائق والمراجع.
- طلبات الملفات.
- المعلمون المرتبطون بسجلات العام.

### عقد القراءة

```text
GET /api/archive/years
GET /api/archive/year?academicYear=2026/2027
```

الأرشيف **قراءة فقط**، ولا يضيف جداول SQLite جديدة. زر «طباعة حزمة التسليم» يحول عرض العام إلى وثيقة A4 قابلة للطباعة من المتصفح.

ملاحظة دقة: لا يستخدم الأرشيف قائمة المعلمين الحالية بوصفها سجلًا تاريخيًا لطاقم كل سنة؛ بل يعتمد الارتباط السنوي الصريح ويستفيد من علاقات السجلات داخل العام المحدد.

## جديد v0.9.0 — مركز التقارير الرسمي

تحول تبويب **التقارير** من placeholder إلى وحدة تشغيلية كاملة تعتمد عقد تقرير موحدًا واحدًا، بدل بناء مولد مستقل لكل وحدة.

### أنواع التقارير

1. التقرير الشامل لأعمال القسم.
2. التقرير المهني للمعلم.
3. تقرير التخطيط ومتابعة المنهج.
4. تقرير التحصيل والنتائج.
5. تقرير الإشراف الفني والزيارات.
6. تقرير الاجتماعات والقرارات.
7. تقرير الفعاليات والتوثيق.

### خصائص مركز التقارير

- فلترة حسب العام الدراسي والفصل.
- اختيار المعلم عند إنشاء تقرير المعلم.
- مؤشرات مشتقة حتميًا من السجلات الأصلية.
- أقسام جدولية موحدة قابلة للتوسع دون تغيير شكل العقد.
- تقرير A4 قابل للطباعة مباشرة من المتصفح.
- إظهار السجلات الفارغة بوضوح بدل اختلاق أرقام بديلة.
- عدم استنتاج مهارات أو أحكام مهنية غير موجودة في البيانات.
- مسار API للقراءة فقط:

```text
GET /api/reports/official
```

مع معاملات:

```text
reportType
academicYear
term
teacherId   # لتقرير المعلم فقط
```

لا ينشئ v0.9.0 أي جدول SQLite جديد ولا يغير البيانات عند إنشاء التقرير.

## البنية التقنية

- **Frontend:** React 19 + TypeScript + Vite.
- **Backend:** FastAPI.
- **Database:** SQLite مع Foreign Keys وWAL.
- **Files:** محلي للتطوير أو Google Drive عبر OAuth ونطاق `drive.file`.
- **CI/CD:** GitHub Actions.
- **Preview:** GitHub Pages.

## التشغيل المحلي

### الخادم

```bash
python -m venv .venv
source .venv/bin/activate
# Windows: .venv\Scripts\activate
pip install -r requirements.txt
uvicorn server.main:app --reload --port 8000
```

### الواجهة

في طرفية ثانية:

```bash
npm install
npm run dev
```

ثم افتح:

```text
http://localhost:5173
```

للبناء الكامل:

```bash
npm run build
uvicorn server.main:app --port 8000
```

إذا كان مجلد `dist` موجودًا، يخدمه FastAPI تلقائيًا.

## Google Drive

الإعداد التفصيلي موجود في [`GOOGLE_DRIVE_SETUP_AR.md`](GOOGLE_DRIVE_SETUP_AR.md).

أهم متغيرات البيئة:

```text
GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET
GOOGLE_REDIRECT_URI
APP_ENCRYPTION_KEY
STORAGE_MODE
```

لا تحتاج v0.14-B إلى SQL يدوي أو تغيير OAuth؛ توجد متغيرات تشغيل جديدة للإنتاج والنسخ الاحتياطي فقط، ولا توجد ترقية SQLite جديدة.

## منهجية التطوير

- GitHub هو مصدر الحقيقة للكود.
- كل إصدار يُبنى فوق آخر `main` أخضر.
- لا تُسلَّم حزمة قبل اختبارها كـOverlay فوق النسخة المستقرة السابقة.
- تغييرات قاعدة البيانات، عند الحاجة، تكون إضافية ومحافظة على البيانات القديمة.
- لا يُعاد بناء منطق سبق أن أصبح مصدر حقيقة في وحدة أخرى.
- نجاح GitHub Actions شرط اعتماد الإصدار كنقطة استقرار رسمية.
- `README.md` في جذر المستودع هو صفحة التعريف الأساسية ويُحدث مع كل مرحلة كبيرة.

## سجل المراحل الرئيسية

- **v0.3:** الملف المهني والسيرة الذاتية.
- **v0.4:** التوثيق الاحترافي للفعاليات.
- **v0.5:** الاجتماعات والقرارات.
- **v0.6:** التخطيط ومتابعة المنهج.
- **v0.7:** الإشراف الفني والزيارات.
- **v0.8:** التحصيل والنتائج.
- **v0.9:** مركز التقارير الرسمي.
- **v0.10:** الأرشيف التاريخي وحزمة التسليم.
- **v0.11:** البحث الشامل والتنقل السريع.
- **v0.12:** دورة التدخل وقياس الأثر مع حارس المرجع العُماني.
- **v0.13:** الإدخال التاريخي والاستمرارية الطولية وتقويم الأعوام.
- **v0.14-A:** جاهزية التشغيل الإنتاجي والنسخ الاحتياطي.
- **v0.14-B:** Railway والتشغيل الحقيقي مع Volume وبوابة دخول مؤقتة.

## حدود المشروع

إدارة المختبرات والأجهزة والمواد الكيميائية ليست ضمن نطاق «مرصد الإنجازات». التركيز هو الإدارة التربوية، المتابعة المهنية، التوثيق، التحصيل، التقارير، والذاكرة المؤسسية للقسم.


### S2-B5 Fix 1 — تصحيح updated_at
كشف اختبار Supabase الحي أن `statement_timestamp()` ثابت طوال SQL statement واحد، لذلك قد لا يتقدم `updated_at` عند تنفيذ أكثر من عملية داخل statement واحد. الإصدار 0.21.1 يضيف Migration تصحيحية جديدة تستبدل الدالة فقط لتستخدم `clock_timestamp()`، مع إبقاء Migration S2-B5 الأصلية والـ22 Trigger دون تعديل.
