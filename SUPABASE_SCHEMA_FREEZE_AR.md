# مرصد الإنجازات — Phase S2-A: PostgreSQL Schema Design Freeze

## 1. هدف المرحلة

تجميد نموذج PostgreSQL المستهدف قبل كتابة أول migration تشغيلية. هذه المرحلة **تصميم + عقد آلي فقط**:

- لا تنشئ جداول في Supabase.
- لا تضيف RLS فعليًا.
- لا تربط React بـ Supabase.
- لا تنقل بيانات SQLite.
- لا تغير منطق FastAPI/SQLite الحالي.

مصدر الحقيقة الآلي للتصميم هو:

```text
supabase/schema/target_schema_v1.json
```

ويفحص عبر:

```bash
python scripts/check_supabase_schema_freeze.py
```

## 2. النتيجة المعمارية المجمدة

المصدر الحالي يملك 25 جدول SQLite. النموذج المستهدف يملك **26 جدول PostgreSQL**. الزيادة ليست نسخًا ميكانيكيًا للجداول، بل إضافة حدود الهوية/المدرسة/العام وإزالة جداول ربط أصبحت غير لازمة.

### جداول جديدة

1. `schools` — جذر العزل بين المدارس.
2. `profiles` — ملف التطبيق المرتبط بـ `auth.users`.
3. `school_memberships` — عضوية المستخدم ودوره داخل المدرسة.
4. `academic_years` — قاموس الأعوام الدراسية لكل مدرسة.

### جداول Legacy ستختفي بعد الهجرة

1. `teacher_record_years` → يندمج في `teacher_years`.
2. `request_record_years` → يصبح `upload_requests.academic_year_id`.
3. `event_record_years` → يصبح `events.academic_year_id`.
4. `event_media_meta` → تندمج `caption/position/is_cover` داخل `event_media`.

## 3. قرار المفاتيح

لا نحول كل شيء إلى UUID لمجرد أن Supabase يستطيع ذلك.

- `auth.users.id`: UUID تديره Supabase.
- `profiles.id`: نفس UUID الخاص بالمستخدم.
- `schools.id`: UUID.
- كيانات التطبيق الموجودة اليوم بأرقام (`teachers`, `meetings`, `events`, ...): `bigint generated by default as identity`.

السبب: الحفاظ على عقود الواجهة الحالية التي تتعامل مع IDs رقمية، وتقليل مساحة التغيير أثناء Vertical Slices. يمكن كذلك إدخال أرقام SQLite القديمة صراحةً أثناء الترحيل ثم متابعة identity sequence بعد أعلى قيمة.

## 4. المدرسة هي Tenant Boundary

كل جدول بيانات مدرسي مكشوف للتطبيق يحمل `school_id` مباشرة، باستثناء `profiles` لأنه يمثل هوية مستخدم يمكن نظريًا أن تنتمي لأكثر من مدرسة.

هذا القرار مقصود لثلاثة أسباب:

1. يجعل RLS لاحقًا واضحًا ومباشرًا.
2. يمنع الاعتماد على سلسلة joins طويلة لمعرفة مالك الصف.
3. يسمح بفهرسة `school_id` ومفاتيح العضوية وتحسين سياسات RLS.

في S2-B ستستخدم العلاقات المدرسية المركبة عند الحاجة لمنع ربط كيان من مدرسة بكيان في مدرسة أخرى.

**قرار v1:** المدرسة هي حد الأمان. المادة/القسم تبقى بيانات مجال وليست Tenant مستقلًا في هذا الإصدار. إذا احتجنا مستقبلًا عزل عدة أقسام مستقلة داخل المدرسة نفسها بصلاحيات منفصلة، فهذا يتطلب Schema Revision صريحة ولا يضاف خلسة داخل RLS.

## 5. Auth والعضوية

العقد المجمد:

```text
auth.users
   ↓ 1:1
profiles
   ↓
school_memberships
   ↓
schools
```

الأدوار المحجوزة للتصميم:

```text
owner
admin
lead_teacher
teacher
viewer
```

وحالات العضوية:

```text
active
invited
suspended
```

ويحتوي `school_memberships.teacher_id` على رابط اختياري إلى سجل المعلم داخل المدرسة. هذا الرابط ضروري عندما يكون الحساب لمعلم أو معلم أول حتى تستطيع سياسات S2-C لاحقًا تمييز «سجل المستخدم نفسه» من بقية سجلات المعلمين. الحساب الإداري الذي لا يمثل معلمًا يمكن أن يترك `teacher_id = NULL`.

لن ننفذ السياسات في S2-A. تنفيذ Auth/RLS الفعلي يؤجل إلى S2-C بعد نجاح مخطط PostgreSQL نفسه.

## 6. الأعوام الدراسية

لن يبقى العام الدراسي نصًا مكررًا في عدة جداول. يصبح لدينا:

```text
academic_years
- id
- school_id
- label       مثال 2026/2027
- start_year  2026
- end_year    2027
- is_current
```

ولا نخترع تواريخ بداية/نهاية للفصول أو العام الدراسي العُماني. المصدر الحالي لا يثبتها، لذلك لا تدخل التصميم.

الجداول الجذرية المرتبطة بسنة محددة:

- `teacher_years`
- `upload_requests`
- `documents`
- `events`
- `meetings`
- `curriculum_plans`
- `supervision_visits`
- `achievement_assessments`

`activities.academic_year_id` يبقى اختياريًا لأن بعض النشاطات النظامية قد لا تكون مرتبطة بسنة سجل محددة.

### توافق السجلات القديمة التي لا تملك سنة صريحة

في Legacy توجد آلية توافق فقط للسجلات القديمة التي فقدت الربط الصريح بالسنة: الفعالية تستخدم تاريخها، والطلب تاريخ إنشائه، والوثيقة تاريخ رفعها، وفق دالة التوافق الحالية التي تفصل عند أغسطس. **هذه ليست قاعدة تربوية جديدة ولا تعريفًا رسميًا لبداية العام العُماني.** في الهجرة:

1. السنة الصريحة تفوز دائمًا.
2. fallback القديم يستخدم فقط عند غياب السنة الصريحة حتى لا نغير الأرشيف القديم بصمت.
3. أي صف أخذ سنته بهذه الطريقة يجب أن يظهر في تقرير الهجرة بصفته `inferred` للمراجعة.

## 7. هوية المعلم مقابل حالته السنوية

هذه أهم عملية تصحيح في S2-A.

### `teachers` — الهوية المستمرة

يحفظ فقط ما يصلح كهوية مهنية مستمرة نسبيًا:

- الاسم
- التخصص
- المؤهل الحالي
- البريد
- الهاتف
- حالة النشاط

### `teacher_profiles`

يحفظ:

- الرقم الوظيفي
- سنة الالتحاق بالمدرسة
- الملخص المهني

### `teacher_years` — الحالة السنوية

يحفظ لكل معلم/مدرسة/عام:

- المادة
- سنوات الخبرة المسجلة لذلك العام
- النصاب
- الصفوف
- المسؤوليات
- حالة الارتباط بالسنة

هذا يمنع تعديل نصاب أو مادة المعلم في 2026/2027 من إعادة كتابة معنى سجلات 2024/2025.

### قاعدة عدم اختلاق التاريخ

المصدر القديم يثبت ارتباط المعلم بالسنة عبر `teacher_record_years` لكنه **لا يثبت تاريخيًا** المادة والنصاب والخبرة والصفوف والمسؤوليات لكل سنة سابقة.

لذلك عند الهجرة:

- صفوف `teacher_years` تبنى من **اتحاد** الارتباط الصريح في `teacher_record_years` وأي ارتباط فعلي للمعلم بسجل سنوي كما يفعل الأرشيف/البحث الحالي (طلب، وثيقة، خطة، زيارة، تقويم، اجتماع، فعالية وما يتبعها).
- العام التشغيلي الذي تملك فيه النسخة الحالية القيم السنوية: تنقل القيم.
- السنوات التاريخية التي لا يملك المصدر قيمها: تبقى الحقول السنوية `NULL`.
- ممنوع نسخ القيم الحالية إلى السنوات القديمة وكأنها حقيقة تاريخية.

## 8. الملفات وSupabase Storage

PostgreSQL يخزن Metadata فقط. الملف نفسه يذهب لاحقًا إلى Supabase Storage.

حقول الوثائق والأدلة المجمدة تشمل:

```text
storage_provider
storage_bucket
storage_path
external_url
```

القيم المقبولة مبدئيًا لمصدر التخزين:

```text
supabase
google_drive
legacy_local
```

هذا يسمح بالحفاظ على Metadata قديمة إن وجدت دون جعل Google Drive جزءًا إلزاميًا من القلب الجديد.

## 9. الأسرار ليست Settings

جدول SQLite الحالي `settings` استُخدم أيضًا لتخزين أشياء حساسة مثل Google OAuth refresh token.

في PostgreSQL يصبح الجدول العام:

```text
school_settings
```

ويحمل **إعدادات غير سرية فقط**.

لا تنقل إليه:

- OAuth refresh token
- OAuth state secret/hash إذا كان ذا وظيفة أمنية مؤقتة
- service-role / secret keys
- provider credentials

أي تكامل سري لاحقًا يمر عبر Edge Function secrets/Vault أو آلية خادمية مناسبة، لا عبر جدول مكشوف للـData API.

## 10. الجداول المستهدفة: 26

### النظام والهوية

- `schools`
- `profiles`
- `school_memberships`
- `academic_years`
- `school_settings`
- `activities`

### المعلمون

- `teachers`
- `teacher_profiles`
- `teacher_years`
- `teacher_cv_items`

### الطلبات والوثائق

- `upload_requests`
- `documents`

### الفعاليات

- `events`
- `event_media`
- `event_teacher_links`

### الاجتماعات

- `meetings`
- `meeting_attendees`
- `meeting_decisions`

### التخطيط

- `curriculum_plans`
- `curriculum_units`

### الإشراف

- `supervision_visits`
- `supervision_actions`

### التحصيل والتدخل

- `achievement_assessments`
- `achievement_assessment_standards`
- `achievement_actions`
- `achievement_action_metrics`

## 11. أنواع الزمن

العقد المجمد:

- وقت حدث حقيقي: `timestamptz` مثل `created_at`, `updated_at`, `expires_at`, `uploaded_at`, `completed_at`.
- يوم تقويمي: `date` مثل تاريخ اجتماع أو زيارة أو اختبار أو موعد استحقاق.
- وقت اجتماع فقط: `time without time zone`.

لا نخزن timestamps كنصوص كما في SQLite.

## 12. القيود التي يجب أن تبقى في PostgreSQL

لا نعتمد على React وحدها لحراسة البيانات.

يجب أن تنقل S2-B قيود المصدر، ومنها:

- النسب بين 0 و100.
- progress بين 0 و100.
- student_count والأعداد الفرعية غير سالبة.
- `mastered + near_mastery + intervention <= student_count`.
- الدرجات المسجلة لا تتجاوز `max_score`.
- `max_score > 0`.
- حالات الاجتماعات والخطط والوحدات والزيارات والتدخلات تبقى ضمن القيم المعتمدة.
- `achievement_action_metrics.direction` يبقى `higher_better` أو `lower_better`.
- `teacher_cv_items.item_type` يبقى ضمن الأنواع الأربعة الحالية.
- token hash لطلبات الرفع Unique.
- غلاف واحد فقط لكل فعالية عبر Partial Unique Index.
- عام جاري واحد فقط لكل مدرسة عبر Partial Unique Index.

## 13. الفهارس

قاعدة S2-B:

1. كل Foreign Key يحتاج فهرسًا ما لم يكن مغطى أصلًا بمفتاح أساسي/Unique مناسب.
2. مفاتيح RLS (`school_id`, عضوية المستخدم) مفهرسة.
3. فهارس النطاق الحالية المهمة تبقى، لكن يعاد بناؤها على `academic_year_id` بدل النص عند الحاجة.
4. لا ننشئ فهارس لمجرد زيادة العدد؛ نثبتها بعقود الاستعلام الحالية ثم نراجع Supabase Performance Advisor لاحقًا.

## 14. ما لا يتغير تربويًا

S2-A لا تغير:

- معنى حد الإتقان.
- قاعدة عدم جمع نسب مبنية على مراجع غير متكافئة.
- منطق baseline/target/outcome.
- أنواع الإجراءات العلاجية/الإثرائية والمتابعة.
- منطق السنوات التاريخية.
- التقارير والبحث.

هذه عقود Parity وليست فرصة «لتبسيط» المنطق أثناء نقل قاعدة البيانات.

## 15. قرار Railway

ملفات Railway في Legacy repository هي تجهيز سابق لم يُستخدم إنتاجيًا. لا تدخل في PostgreSQL schema ولا تمثل مصدر بيانات. تبقى مؤقتًا فقط حتى مرحلة تنظيف Legacy مستقلة كي لا نخلط تنظيف الملفات مع بناء Supabase.

## 16. معيار إغلاق S2-A

يجب أن ينجح:

```bash
python -m pytest -q
python scripts/check_marsad_baseline.py
python scripts/check_supabase_foundation.py
python scripts/check_supabase_schema_freeze.py
python scripts/marsad_e2e_regression.py
```

مع الشروط التالية:

- SQLite = 25 جدولًا كما هو.
- PostgreSQL target contract = 26 جدولًا.
- 25/25 جداول Legacy لها قواعد تحويل صريحة.
- لا SQL migration تطبيقية بعد.
- لا Supabase runtime consumer بعد.
- لا تعديل React/منطق تربوي.

بعد ذلك تبدأ **S2-B — Core PostgreSQL Migration**، وهي أول مرحلة تكتب SQL فعليًا.
