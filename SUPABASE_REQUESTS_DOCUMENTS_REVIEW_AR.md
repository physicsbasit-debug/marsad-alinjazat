# S3-C3A — طلبات الملفات وفهرس الوثائق

تنقل هذه المرحلة **إدارة القراءة والمراجعة** للعام الدراسي الجاري إلى Supabase دون القفز إلى الرفع العام أو Storage.

## ما انتقل
- قائمة طلبات الملفات من `upload_requests` عبر RLS.
- تحديث حالة الطلب عبر `marsad_update_upload_request_status_v1`.
- عند اعتماد الطلب، تُعتمد metadata الوثائق المرتبطة به في `documents`.
- قائمة الوثائق وبياناتها الوصفية من Supabase.
- عداد الطلبات المفتوحة في القائمة الجانبية من Supabase.

## ما لم ينتقل
- إنشاء طلب جديد ورابط رفع عام.
- صفحة الرفع العام بالرمز Token.
- رفع الملفات إلى Supabase Storage.
- الرفع المباشر للوثائق.

هذه الحدود مقصودة حتى لا يُنشئ النظام رابط رفع لا يملك مسار تخزين حيًا بعد.
