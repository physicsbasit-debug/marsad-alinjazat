# S3-C1 — حدود قراءة المجالات المرتبطة بالمعلم

الإصدار: `v0.31.0`

توسّع هذه المرحلة صفحة المعلمين الحالية فقط لتقرأ العلاقات التي ترتبط بمعرف المعلم السحابي نفسه من Supabase:

- طلبات الملفات `upload_requests`
- الوثائق `documents`
- الزيارات الإشرافية `supervision_visits`
- إجراءات متابعة الزيارات `supervision_actions`

## القاعدة الحاسمة

لا توجد مطابقة بين أرقام معلمي Legacy وأرقام معلمي Supabase، ولا مطابقة أسماء أو بريد أو تخصص. العلاقة المقبولة الوحيدة هي مفتاح `teacher_id` الموجود أصلًا داخل جداول Supabase والمقيد بمفاتيح PostgreSQL وRLS.

لذلك لا تُدمج السجلات القديمة الموجودة في SQLite داخل عدادات معلم Supabase. ظهور صفر يعني: لا توجد سجلات سحابية مرتبطة بهذا المعلم في العام الحالي، وليس أن السجلات القديمة اختفت.

## حدود المرحلة

هذه مرحلة قراءة فقط للمجالات المرتبطة. إنشاء طلب ملف، رفع وثيقة، إنشاء زيارة، أو تعديل هذه المجالات يبقى على المسار السابق حتى مراحل القطع الخاصة بها.

لا Migration جديدة، ولا تعديل RLS، ولا حذف معلمين، ولا تغيير لبقية صفحات المرصد.
