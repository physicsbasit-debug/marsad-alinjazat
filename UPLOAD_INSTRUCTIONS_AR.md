# تعليمات رفع مرصد الإنجازات v0.9.0 — مركز التقارير الرسمي

هذه الحزمة مبنية فوق آخر نقطة استقرار خضراء في `main`:

```text
30c21626b828a5653569832f8a30bbb1566fe811
```

## ما الذي ترفعه؟

استخدم حزمة **Changed Files Only** وارفع محتوياتها إلى **جذر المستودع** مع استبدال الملفات الموجودة عند طلب GitHub. لا ترفع مجلدًا أبًا إضافيًا.

## نطاق التغيير

- إضافة صفحة `src/pages/Reports.tsx`.
- تحويل تبويب «التقارير» إلى مركز تقارير فعلي.
- إضافة عقد TypeScript موحد للتقارير.
- إضافة API قراءة فقط `/api/reports/official`.
- دعم سبعة تقارير رسمية: القسم، المعلم، التخطيط، التحصيل، الإشراف، الاجتماعات، الفعاليات.
- دعم العام الدراسي والفصل، واختيار المعلم في تقرير المعلم.
- إضافة تخطيط A4 للطباعة من المتصفح.
- تحديث `README.md` و`README_AR.md` لشرح v0.9.0.
- تحديث اختبارات API لعقد التقارير.

## ما الذي لم يتغير؟

- لا تغيير في `server/db.py` أو مخطط SQLite.
- لا SQL يدوي ولا migration.
- لا تغيير في Google Drive أو OAuth.
- لا أسرار جديدة.
- لا تغيير في GitHub Pages أو Workflow أو Vite أو tsconfig أو requirements.
- لا تغيير في منطق التخطيط أو التحصيل أو الإشراف أو الاجتماعات أو الفعاليات أو الطلبات.

## بعد الرفع

انتظر تشغيل **Quality & Live Preview**، ويجب أن تنجح الوظائف الثلاث:

```text
quality
build-preview
deploy-preview
```

بعد الأخضر افتح المعاينة الحية ثم تبويب **التقارير**، وجرّب:

1. تقرير القسم الشامل.
2. تقرير المعلم مع تغيير المعلم.
3. تقرير التحصيل والتخطيط والإشراف.
4. زر «طباعة التقرير» ومعاينة A4.

لا تحتاج إلى تغيير أي إعداد آخر.
