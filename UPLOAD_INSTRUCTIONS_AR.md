# تعليمات رفع مرصد الإنجازات v0.14-A — جاهزية التشغيل الإنتاجي والنسخ الاحتياطي

هذه الحزمة مبنية فوق آخر نقطة استقرار خضراء في `main`:

```text
55ceae6afe2f33635019a9cd07a85a428973db32
```

## ما الذي ترفعه؟

استخدم حزمة **Changed Files Only** وارفع **محتوياتها** إلى جذر المستودع مع استبدال الملفات المطابقة. لا ترفع المجلد الأب نفسه.

## نطاق المرحلة

- إضافة `VITE_API_BASE_URL` حتى تدعم الواجهة Same Origin أو Backend منفصلًا دون إعادة كتابة استدعاءات API.
- حل روابط صور وأدلة الفعاليات عبر عنوان الـAPI نفسه.
- دعم المسار الأساسي لـVite في رابط الرفع العام وعودة OAuth داخل الواجهة.
- إضافة CORS مضبوط عبر `APP_CORS_ORIGINS` و`APP_FRONTEND_URL`.
- إضافة `/api/ready` لفحص قاعدة البيانات ومجلدات البيانات والنسخ والرفع، مع حارس خاص بوضع الإنتاج.
- إنشاء نسخة SQLite متسقة عند بدء التشغيل قبل `init_db()` عندما توجد قاعدة قائمة، باستخدام SQLite Backup API.
- إضافة سياسة احتفاظ بالنسخ وحد أدنى زمني بين نسخ بدء التشغيل.
- إضافة `scripts/marsad_maintenance.py` لإنشاء النسخة، فحصها، والاستعادة Offline مع نسخة أمان قبل الاستبدال.
- إضافة وثيقة نشر إنتاجي وقالب متغيرات بيئة واضح.
- إضافة `.gitignore` لمنع رفع قواعد البيانات والأسرار والمجلدات التشغيلية بالخطأ.

## ما الذي لم يتغير؟

- لا جدول SQLite جديد.
- لا SQL يدوي.
- لا تغيير في منطق التحصيل أو الإتقان أو التدخلات أو أي حكم تربوي.
- لا تغيير في Google Drive أو نطاق OAuth نفسه.
- لا تعديل GitHub Workflow؛ Pages تبقى معاينة بـ`VITE_PREVIEW_MODE=true`.
- لا يوجد Endpoint HTTP للاستعادة؛ الاستعادة Server-side فقط حتى توجد مصادقة وصلاحيات إنتاجية.

## بعد الرفع

1. انتظر **Quality & Live Preview** حتى يصبح أخضر.
2. افتح المعاينة وتأكد أن الوظائف السابقة لم تتراجع؛ الحفظ سيبقى معطلًا على GitHub Pages عمدًا.
3. لا تغيّر `VITE_PREVIEW_MODE` في Pages إلى `false`.
4. المرحلة اللاحقة v0.14-B ستختار بيئة FastAPI ذات تخزين دائم وتختبر الحفظ بعد إعادة تشغيل الخدمة.

## ملاحظة تشغيلية

ملف `PRODUCTION_ENV_TEMPLATE.txt` قالب فقط. لا تضع أسرارك الحقيقية داخل المستودع. في الإنتاج يجب أن يكون `APP_DATA_DIR` و`APP_BACKUP_DIR` على Volume/Persistent Disk فعلي، وليس على قرص مؤقت داخل الحاوية.
