# S3-C2 — تحويل الإشراف والزيارات إلى Supabase

الإصدار: **v0.32.0**

## الهدف
تحويل مسار **الإشراف والمتابعة** للعام الدراسي الجاري من Legacy إلى Supabase مع إبقاء الرجوع اليدوي إلى Legacy، دون نقل بقية مجالات المرصد.

## النطاق التشغيلي
- قراءة `supervision_visits` و`supervision_actions` من Supabase عبر RLS.
- قراءة السجل الزمني من `activities`.
- إنشاء وتحديث الزيارة عبر RPC ذرية.
- إنشاء وتحديث وحذف إجراء المتابعة عبر RPC ذرية.
- الحفاظ على `closed_at` عند إغلاق/إعادة فتح الزيارة.
- الحفاظ على `completed_at` عند إكمال/إعادة فتح إجراء المتابعة.
- العام الجاري فقط؛ الأعوام التاريخية تبقى Legacy.
- نتيجة بحث Legacy العميقة تعيد مسار الإشراف مؤقتًا إلى Legacy بدل خلط المعرفات.

## الصلاحيات
- القراءة في واجهة S3-C2: `owner`, `admin`, `lead_teacher`.
- الكتابة: `owner`, `admin` فقط.
- `lead_teacher` يبقى للقراءة فقط في هذه المرحلة.
- حذف الزيارة نفسها غير مفعّل.
- حذف إجراء المتابعة يبقى ضمن السطح المقبول.

## Migration
`20260904194500_s3_c2_supervision_write_cutover.sql`

لا تضيف جدولًا أو عمودًا جديدًا. تضيف فقط:
1. سياسة/منح INSERT محدودة على `activities` للإدارة.
2. خمس RPCs بصلاحية `SECURITY INVOKER` حتى تبقى صلاحيات المستخدم وRLS هي المرجع.

## الترتيب الحي
1. رفع حزمة changed files إلى GitHub.
2. انتظار `Quality & Live Preview` حتى GREEN.
3. تشغيل Migration مرة واحدة في Supabase SQL Editor.
4. تشغيل `supabase/tests/s3_c2_live_acceptance.sql`؛ الاختبار ينتهي بـ`ROLLBACK`.
5. فتح GitHub Pages ثم «الإشراف والمتابعة» والتحقق من ظهور `S3-C2 • تشغيل الإشراف عبر Supabase`.
6. تسجيل زيارة حقيقية، تعديلها، إضافة إجراء متابعة وتحديثه، ثم تحديث الصفحة والتأكد من بقاء البيانات.

## خارج النطاق
طلبات الملفات، الوثائق، الاجتماعات، التخطيط، التحصيل، الفعاليات، Storage، Public Upload، وحذف الزيارة نفسها لا تتحول في S3-C2.
