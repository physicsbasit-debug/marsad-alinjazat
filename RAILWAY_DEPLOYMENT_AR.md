# تشغيل مرصد الإنجازات على Railway — v0.14-B

هذه هي بيئة التشغيل الإنتاجية الأولى المعتمدة للمشروع. النمط المستخدم **نطاق واحد**: FastAPI يخدم الواجهة المبنية وواجهات `/api` من نفس خدمة Railway، لذلك لا نحتاج CORS بين خدمتين ولا عنوان API منفصل.

## قبل البدء

- GitHub Pages تبقى معاينة فقط.
- Railway هي التي ستحتوي الخدمة الحقيقية وقاعدة SQLite الدائمة.
- لا تستخدم الخدمة بلا Volume. ملف SQLite داخل حاوية بلا Volume سيضيع عند إعادة النشر.

## 1. أنشئ مشروع Railway من GitHub

من Railway:

1. `New Project`.
2. `Deploy from GitHub repo`.
3. اختر `physicsbasit-debug/marsad-alinjazat`.
4. إذا بدأ Deploy أولي وفشل عند `/api/ready` قبل إضافة Volume فهذا متوقع؛ حارس الجاهزية يرفض تشغيل إنتاج بلا تخزين دائم.

المشروع يحتوي `Dockerfile` و`railway.json`، لذلك لا تحتاج كتابة Build Command أو Start Command يدويًا.

## 2. أضف Volume دائمًا

اربط Volume واحدًا بالخدمة واضبط Mount Path إلى:

```text
/app/persist
```

Railway يمرر المسار تلقائيًا في `RAILWAY_VOLUME_MOUNT_PATH`. يكتشفه المرصد ويستخدم داخله:

```text
/app/persist/data
/app/persist/backups
/app/persist/uploads/inbox
/app/persist/uploads/events
```

لا تضف `APP_DATA_DIR` أو بقية مسارات التخزين يدويًا إلا إذا أردت تجاوز هذا السلوك عن قصد.

**لا ترفع عدد Replicas فوق 1 مع SQLite + Volume.**

## 3. أضف متغيرات التشغيل

افتح `Variables > RAW Editor` والصق محتوى `RAILWAY_ENV_TEMPLATE.txt`، ثم غيّر `APP_ACCESS_PASSWORD` إلى كلمة مرور قوية خاصة بك قبل Deploy. لا تستخدم القيمة الافتراضية.

في أول تشغيل استخدم:

```text
STORAGE_MODE=local
```

حتى نغلق اختبار الاستمرارية. Google Drive يفعّل في خطوة مستقلة بعد ذلك.

## 4. أنشئ النطاق العام

من `Settings > Networking` اختر `Generate Domain`.

Railway يزوّد التطبيق تلقائيًا بـ`RAILWAY_PUBLIC_DOMAIN`. في v0.14-B يستخدمه المرصد تلقائيًا لبناء `APP_PUBLIC_URL` و`APP_FRONTEND_URL` عندما لا تحددهما يدويًا.

بعد إنشاء النطاق نفّذ Redeploy مرة واحدة حتى تبدأ العملية الجديدة بالقيمة النهائية للنطاق.

## 5. بوابة الدخول والجاهزية

عند فتح النطاق في وضع الإنتاج ستظهر صفحة دخول بسيطة. هذه بوابة مؤقتة أحادية المستخدم حتى نبني الحسابات والصلاحيات الكاملة. كل API الإداري محمي بالجلسة، بينما روابط رفع المعلمين ذات token تبقى عامة كما صُممت.


افتح:

```text
https://YOUR_DOMAIN/api/health
```

ثم:

```text
https://YOUR_DOMAIN/api/ready
```

يجب أن يعيد `/api/ready` HTTP 200 و`ok: true`.

إذا أعاد 503 فلا تدخل بيانات حقيقية. افحص الحقول داخل `checks`؛ غالبًا Volume غير مربوط أو مساره غير قابل للكتابة.

## 6. اختبار الاستمرارية الإلزامي

من رابط Railway نفسه، لا GitHub Pages:

1. اختر عامًا دراسيًا.
2. أنشئ اجتماعًا.
3. اختر أسماء الحضور.
4. احفظ الاجتماع.
5. افتحه مرة أخرى وتأكد من الحضور.
6. من Railway أعد تشغيل الخدمة `Restart`.
7. بعد عودتها افتح نفس الاجتماع.
8. يجب أن يبقى الاجتماع والحضور كما هما.

ثم كرر اختبارًا مختصرًا لخطة، نتيجة تحصيل، زيارة، فعالية، وثيقة، وسنة تاريخية.

نجاح Build وحده لا يغلق v0.14-B. **الاستمرار بعد Restart هو معيار القبول.**

## 7. النسخ الاحتياطية

المرصد ينشئ نسخ SQLite داخل `/app/persist/backups` وفق إعدادات v0.14-A.

Railway يدعم أيضًا Backups للـVolume نفسه. بعد نجاح اختبار الاستمرارية فعّل من تبويب `Backups` نسخة دورية مناسبة. نسخة Railway تحمي محتوى الـVolume بالكامل، بينما نسخ المرصد الداخلية تحمي SQLite بصورة متسقة وقابلة للفحص والاستعادة بأداة الصيانة.

## 8. Google Drive لاحقًا

بعد إغلاق اختبار الاستمرارية يمكن تغيير:

```text
STORAGE_MODE=google_drive
```

ثم إضافة أسرار Google. لا تغيّرها قبل إثبات أن قاعدة البيانات نفسها دائمة.

## حدود هذا النشر

- خدمة واحدة وReplica واحدة.
- SQLite مناسب لحجم واستخدام مرصد قسم مدرسي بهذه البنية.
- GitHub Pages لا تصبح واجهة الإنتاج؛ رابط Railway هو رابط التشغيل الحقيقي في v0.14-B.
- لا توجد طبقة تسجيل دخول وصلاحيات بعد، لذلك لا تنشر الرابط الإنتاجي على نطاق عام واسع قبل مرحلة المصادقة إذا كانت البيانات حقيقية وحساسة.
