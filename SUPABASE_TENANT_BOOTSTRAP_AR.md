# S2-E2 — تهيئة المستأجر الإنتاجي الأول

الإصدار: **v0.26.0**

هذه المرحلة تنشئ الحد الأدنى الحقيقي لبيئة المدرسة في Supabase بعد نجاح اختبارات قاعدة البيانات وDry Run التمثيلي.

## ما الذي يُنشأ فعليًا؟

فقط:

1. سجل المدرسة في `schools`.
2. إسقاط آمن لحساب Supabase Auth الموجود إلى `profiles` عند الحاجة فقط.
3. عضوية المالك في `school_memberships` بدور `owner` وحالة `active`.
4. العام الدراسي الحالي `2026/2027` في `academic_years` مع `is_current=true`.

لا تُنشأ بيانات معلمين أو اجتماعات أو فعاليات أو وثائق أو نتائج أو سجلات تجريبية.

## لماذا لا نزرع school_settings الآن؟

لا توجد في العقد الحالي مفاتيح إعدادات تشغيل إلزامية معتمدة لهذا المسار. لذلك تُترك `school_settings` فارغة إلى أن يعتمد Runtime Contract مفاتيحها صراحةً. هذا يمنع تكوين إعدادات افتراضية غير موثقة ثم الاضطرار لدعمها لاحقًا.

## الخصوصية

المستودع عام، لذلك لا يُحفظ بريد حساب المالك أو نسخة SQL الشخصية داخله. الملف:

`supabase/bootstrap/s2_e2_tenant_bootstrap_template.sql`

يحتوي placeholders فقط. النسخة الحية المخصصة تُسلّم خارج GitHub.

## الأمان وإعادة التشغيل

Bootstrap مصمم ليكون آمنًا عند إعادة تشغيله لنفس المدرسة والمالك والعام. لكنه يتوقف بدل التخمين إذا وجد:

- أكثر من مدرسة بالاسم نفسه.
- المدرسة الموجودة غير نشطة.
- عضوية الحساب موجودة لكن ليست `owner/active`.
- سنة حالية أخرى متعارضة.
- سنة `2026/2027` موجودة بحدود سنوات غير متسقة.

أي خطأ قبل `COMMIT` يلغي العملية كاملة.

## اختبار RLS بعد Bootstrap

ملف القبول المنفصل يختبر داخل transaction ينتهي بـ`ROLLBACK`:

- المالك يرى مدرسته وعضويته وسنته الحالية.
- المالك لا يرى tenant آخر مؤقتًا.
- helper الخاص بالدور يقبل `owner` الحقيقي.
- تحديث المدرسة المسموح للمالك يمر تحت RLS.
- هوية authenticated عشوائية بلا عضوية لا ترى المدرسة ولا العضويات ولا السنوات.

## ما الذي لا يعنيه نجاح S2-E2؟

نجاح المرحلة يعني فقط أن **Tenant الحقيقي وحدود المالك جاهزة في Supabase**. لا يزال React يعمل وفق المسار القديم، ولا يوجد Runtime cutover أو Storage cutover أو Public Upload cutover في هذه المرحلة.
