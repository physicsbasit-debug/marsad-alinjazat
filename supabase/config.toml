# Supabase local development configuration. S2-B migrations exist, but the application runtime has not switched from FastAPI/SQLite yet.
project_id = "marsad-alinjazat"

[auth]
enabled = true
site_url = "http://localhost:5173"
additional_redirect_urls = ["http://localhost:5173/**"]

[functions.marsad-public-upload]
verify_jwt = false
